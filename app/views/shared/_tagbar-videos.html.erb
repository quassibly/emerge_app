<div class="tagbar-container">
  <div class="tagbar">
    <div class="hidden-xs hidden-sm">
      <%= link_to "All", videos_path, class: "tagbar-link blank" %>
      <% Article.where(category: "video").where(published: true).where(deleted: false).pluck(:tag).uniq.each do |tag| %>
        <%= link_to tag, videos_path(tag: tag), class: current_tag?(tag) %>
      <% end %>
    </div>
  </div>
</div>
