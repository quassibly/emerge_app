<div class="event-header-photo" style="background-image: url('<%= @pin.photo %>')">
  <div></div>
</div>
  <div class="event-head-card">
    <h1 class="profile-headline upcase"><%= @pin.name %></h1>
    <div class="card-subhead"><%= "#{@pin.date.strftime("%-d.%-m.%Y")} - #{@pin.end_date.strftime("%-d.%-m.%Y")}" %></div>
    <h2 class="profile-subhead"><%= @pin.location %></h2>
    <div class=" "><%= @pin.url %></div>
  </div>

<div class="profile-container">
  <div class="profile-body"><%= @pin.description.html_safe
    %>
  </div>
</div>

<script>
  var imgs = document.querySelectorAll('.profile-body img');
  var divs = document.querySelectorAll('.profile-body div');
  var videos = document.querySelectorAll('.profile-body video');
  console.log(imgs);
  console.log(divs);

  [].forEach.call(divs, function(div) {
    div.classList.add("body");
  });

  [].forEach.call(imgs, function(img) {
    img.closest("div").classList.remove("body");
    img.closest("div").classList.add("wide");
  });

  [].forEach.call(videos, function(video) {
    video.closest("div").classList.remove("body");
    video.closest("div").classList.add("video");
  });
</script>


